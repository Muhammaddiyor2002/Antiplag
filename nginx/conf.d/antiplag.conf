upstream antiplag_app {
  server app:3000;
}

# HTTP — redirect to HTTPS (when SSL configured), or serve directly
server {
  listen 80 default_server;
  server_name _;

  # ACME challenge for Let's Encrypt
  location /.well-known/acme-challenge/ {
    root /var/www/certbot;
  }

  # Uncomment after SSL is configured:
  # return 301 https://$host$request_uri;

  location / {
    proxy_pass http://antiplag_app;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
  }
}

# HTTPS — uncomment and edit yourdomain.uz after running certbot
# server {
#   listen 443 ssl http2;
#   server_name yourdomain.uz www.yourdomain.uz;
#
#   ssl_certificate /etc/letsencrypt/live/yourdomain.uz/fullchain.pem;
#   ssl_certificate_key /etc/letsencrypt/live/yourdomain.uz/privkey.pem;
#   ssl_protocols TLSv1.2 TLSv1.3;
#
#   add_header Strict-Transport-Security "max-age=31536000; includeSubDomains" always;
#   add_header X-Frame-Options "SAMEORIGIN" always;
#   add_header X-Content-Type-Options "nosniff" always;
#   add_header X-XSS-Protection "1; mode=block" always;
#   add_header Referrer-Policy "strict-origin-when-cross-origin" always;
#
#   location / {
#     proxy_pass http://antiplag_app;
#     proxy_http_version 1.1;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto $scheme;
#     proxy_set_header Upgrade $http_upgrade;
#     proxy_set_header Connection "upgrade";
#   }
# }
